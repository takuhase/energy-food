<% per_order.each do |date_food_relation| %>
  <ul class="list-group list-group-flush">
    <li class="list-group-item  <%= "bg-secondary order-fixed" unless Time.current < per_order.first.date %>">
      <div class="row">
        <div class="row col-sm-10 col-9">
          <h6 class="col-sm-4">
            <%= date_food_relation.chinese_name %>
          </h6>
          <h6 class="col-sm-6">
            <%= date_food_relation.english_name %>
          </h6>
          <h6 class="col-sm-2">
            <%= date_food_relation.alacarte_price %>円
          </h6>
        </div>
        <i class="col-sm-2 col-3">
          <%= render 'likes/shared/like', food: date_food_relation.food %>
        </i>
      </div>
    </li>
  </ul>
<% end %>
